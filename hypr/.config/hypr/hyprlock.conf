general {
    disable_loading_bar = true
    hide_cursor = true
    grace = 0
    no_fade_in = false
}

background {
    monitor =
    path = /home/blasley/Pictures/catdj2.png
    blur_passes = 2
    blur_size = 3
}

input-field {
    monitor =
    size = 300, 50
    outline_thickness = 3
    dots_size = 0.26
    dots_spacing = 0.15
    dots_center = true
    dots_rounding = -1
    outer_color = rgb(542883)
    inner_color = rgb(1a1a2e)
    font_color = rgb(e0e0e0)
    fade_on_empty = true
    fade_timeout = 1000
    placeholder_text = <i>Password...</i>
    hide_input = false
    rounding = 10
    check_color = rgb(542883)
    fail_color = rgb(ff5555)
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
    fail_transition = 300
    capslock_color = -1
    numlock_color = -1
    bothlock_color = -1
    invert_numlock = false
    swap_font_color = false
    position = 0, -200
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] echo "<b><big> $(date +"%H:%M") </big></b>"
    color = rgb(ffffff)
    font_size = 72
    font_family = JetBrains Mono Nerd Font
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgb(542883)
    position = 0, 16
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:18000000] echo "<b> $(date +'%A, %-d %B %Y') </b>"
    color = rgb(c9a0dc)
    font_size = 26
    font_family = JetBrains Mono Nerd Font
    shadow_passes = 1
    shadow_size = 3
    position = 0, -48
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:43200000] echo "  ／l、
（ﾟ､ ｡ ７
  l  ~ヽ
  じしf_,)ノ"
    color = rgb(ff64ff)
    font_size = 14
    font_family = JetBrains Mono Nerd Font
    shadow_passes = 3
    shadow_size = 6
    shadow_color = rgb(ff64ff)
    position = -400, 200
    halign = center
    valign = center
}

label {
    monitor =
    text = <b>♪♫ Why not just live in the moment, especially if it has a good beat? ♫♪</b>
    color = rgb(00ffff)
    font_size = 18
    font_family = JetBrains Mono Nerd Font
    shadow_passes = 2
    shadow_size = 4
    shadow_color = rgb(542883)
    position = 0, 180
    halign = center
    valign = center
}

label {
    monitor =
    text = <b>✨ Welcome back, $USER! ✨</b>
    color = rgb(ffd700)
    font_size = 24
    font_family = JetBrains Mono Nerd Font
    shadow_passes = 2
    shadow_size = 3
    shadow_color = rgb(542883)
    position = 0, 145
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:5000] echo "<b>🔋 $(cat /sys/class/power_supply/BAT*/capacity 2>/dev/null || echo 'AC')%  |  📡 $(nmcli -t -f DEVICE,STATE device | grep ':connected' | wc -l) connected</b>"
    color = rgb(e599ff)
    font_size = 16
    font_family = JetBrains Mono Nerd Font
    shadow_passes = 1
    shadow_size = 2
    position = 0, -280
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:60000] echo "<b>$(uptime -p | sed 's/up /⏱ Uptime: /')</b>"
    color = rgb(00ffff)
    font_size = 14
    font_family = JetBrains Mono Nerd Font
    shadow_passes = 1
    shadow_size = 2
    position = 0, -310
    halign = center
    valign = center
}

# Weather widget
label {
    monitor =
    text = cmd[update:1800000] curl -s 'wttr.in/Alexandria,VA?format=%c+%f+%w&u' 2>/dev/null | sed 's/^/🌍 Alexandria: /' || echo "🌍 Weather unavailable"
    color = rgb(87ceeb)
    font_size = 16
    font_family = JetBrains Mono Nerd Font
    shadow_passes = 2
    shadow_size = 3
    shadow_color = rgb(542883)
    position = -30, -350
    halign = center
    valign = center
}

# Fun fact of the day
label {
    monitor =
    text = cmd[update:43200000] sh -c 'curl -s "https://uselessfacts.jsph.pl/api/v2/facts/random" 2>/dev/null | grep -o "\"text\":\"[^\"]*" | sed "s/\"text\":\"/💡 /" | head -c 90 || fortune -s 2>/dev/null | head -n1 | head -c 80 | sed "s/^/💡 /" || echo "💡 Facts make everything better!"'
    color = rgb(98fb98)
    font_size = 13
    font_family = JetBrains Mono Nerd Font
    shadow_passes = 1
    shadow_size = 2
    position = 0, -430
    halign = center
    valign = center
}
