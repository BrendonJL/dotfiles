# Secrets - never commit these
zsh/.secrets
*.secrets
secrets.conf

# DMS plugin repos (will be re-downloaded)
dms/.config/DankMaterialShell/plugins/.repos/

# Yazi plugins (managed by ya pkg)
yazi/.config/yazi/plugins/
yazi/.config/yazi/package.toml

# Cache and temp files
*.swp
*.swo
*~
.DS_Store
